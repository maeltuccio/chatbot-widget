<!DOCTYPE html>
<html lang="en">
  <head>
    <title>ChatbotSaas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="backoffice-shell">
    <aside class="app-sidebar">
      <%= link_to root_path, class: "app-brand" do %>
        <span class="app-brand-mark">AI</span>
        <span>
          <strong>Chatbot SaaS</strong>
          <small>Backoffice</small>
        </span>
      <% end %>

      <nav class="app-nav" aria-label="Backoffice navigation">
        <%= link_to "Agents", agents_path, class: "app-nav-link" %>
        <%= link_to "Usage", usage_path, class: "app-nav-link" %>
      </nav>
    </aside>

    <div class="app-frame">
      <header class="topbar">
        <div>
          <span class="topbar-kicker">Workspace</span>
          <strong><%= current_account&.name || "AI Operations Console" %></strong>
        </div>
        <div class="actions">
          <% if user_signed_in? %>
            <span class="topbar-status"><%= current_user.email %></span>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-primary" %>
          <% end %>
        </div>
      </header>

      <main class="app-main">
        <% if notice.present? %>
          <div class="flash flash-notice"><%= notice %></div>
        <% end %>

        <% if alert.present? %>
          <div class="flash flash-alert"><%= alert %></div>
        <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>
