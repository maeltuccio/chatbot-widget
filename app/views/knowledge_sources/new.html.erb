<section class="page-header">
  <div>
    <span class="eyebrow">Knowledge</span>
    <h1 class="page-title">New source</h1>
    <p class="page-description">Add manual knowledge for <%= @agent.name %>.</p>
  </div>

  <div class="actions">
    <%= link_to "Back", agent_knowledge_sources_path(@agent), class: "btn" %>
  </div>
</section>

<%= render "form", agent: @agent, knowledge_source: @knowledge_source %>
