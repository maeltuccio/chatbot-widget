<section class="page-header">
  <div>
    <span class="eyebrow">Conversations</span>
    <h1 class="page-title"><%= @agent.name %></h1>
    <p class="page-description">Inspect visitor sessions and review message history.</p>
  </div>

  <div class="actions">
    <%= link_to "Back to agent", agent_path(@agent), class: "btn" %>
  </div>
</section>

<section class="table-wrap">
  <table>
    <thead>
      <tr>
        <th>Token</th>
        <th>Messages</th>
        <th>Last message</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @conversations.each do |conversation| %>
        <tr>
          <td><code><%= conversation.public_token %></code></td>
          <td><%= conversation.messages.size %></td>
          <td><%= l((conversation.last_message_at || conversation.created_at), format: :short) %></td>
          <td><%= link_to "Open", agent_conversation_path(@agent, conversation), class: "btn" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</section>
